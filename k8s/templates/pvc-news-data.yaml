apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "malhaebang.fullname" . }}-news-data
  labels:
    {{- include "malhaebang.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: gp2 