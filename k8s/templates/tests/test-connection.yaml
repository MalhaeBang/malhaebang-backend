apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "malhaebang.fullname" . }}-test-connection"
  labels:
    {{- include "malhaebang.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "malhaebang.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
